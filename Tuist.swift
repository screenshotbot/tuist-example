import ProjectDescription

// Project-wide Tuist configuration. Everything this sample needs lives in
// `Project.swift`; the Tuist server integration (`fullHandle:`) is optional and
// unrelated to Screenshotbot.
let tuist = Tuist()
